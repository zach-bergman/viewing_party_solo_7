<h1>Discover Movies</h1>

<%= button_to "Discover Top Rated Movies", user_movies_path(@user.id), method: :get, data: { turbo: false } %>
<%= form_with url: user_movies_path(@user.id), method: :get, data: { turbo: false } do |f| %>
  <%= f.text_field :search %>
  <%= f.submit "Search" %>
<% end %>