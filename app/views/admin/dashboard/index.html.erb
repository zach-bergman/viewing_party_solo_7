<h2>Existing Users</h2>
  <ul id="default_users">
  <% @users.each do |user| %>
    <li><%= link_to user.email, admin_user_path(user), method: :get, class: "btn btn-lg btn-outline-secondary" %></li>
  <% end %>
</ul>